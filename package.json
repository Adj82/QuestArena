{
  "name": "questarena",
  "version": "1.0.0",
  "scripts": {
    "build": "if [ ! -d \"flutter\" ]; then git clone -b stable --depth 1 https://github.com/flutter/flutter.git; fi && ./flutter/bin/flutter build web --release && mkdir -p public && cp -r build/web/* public/"
  }
}
